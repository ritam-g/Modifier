# Leave empty when frontend and backend are served from the same domain (Render static + API on one host).
VITE_API_BASE_URL=https://modifier.onrender.com/

# Local dev example:
# VITE_API_BASE_URL=http://localhost:3000
